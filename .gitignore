__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local scratch output — uv.lock IS committed on purpose, do not add it here.
out/
raw/
*.local.csv

# Archives and evidence produced by running the tool in a checkout. A zero-config
# `eufinreg ingest` writes ./eufinreg-store, and one GISA snapshot alone is 58 MB.
eufinreg-store/
store/
snapshots/
*.jsonl.sha256
receipt-*.zip
doctor.json

# Harness session state — never belongs in the repository.
.claude/
